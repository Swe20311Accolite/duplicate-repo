pipeline {
    agent any
    stages {
        stage('clean-workspace'){
            steps{
                cleanWs()
            }
        }
        stage('test'){
            steps{
                sh 'echo ${params.branch}'
                sh 'cat kkpod.yaml'
            }
        }
    }
}
